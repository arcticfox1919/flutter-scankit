

import 'package:flutter_easy_permission/easy_permissions.dart';

const permissions = const [
  Permissions.READ_EXTERNAL_STORAGE,
  Permissions.CAMERA
];

const permissionGroup = const [
  PermissionGroup.Camera,
  PermissionGroup.Photos
];